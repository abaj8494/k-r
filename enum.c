#include <stdio.h>


enum boolean {ON, OFF};
enum months {JAN = 1, FEB, MAR, APR, MAY, JUN, JUL, AUG,
             SEP, OCT, NOV, DEC};

int main()
{
    printf("%d\n", OFF);
    printf("%d\n", OCT);
}
